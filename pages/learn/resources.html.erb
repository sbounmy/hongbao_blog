---
title: Bitcoin Learning Center - Hong₿ao
description: Everything you need to know about Bitcoin
layout: site
---

<%
  sections = {
    beginner: [
      { title: "What is Bitcoin?", duration: "3 min read", icon: "📖", path: "#" },
      { title: "Your First Bitcoin Transaction", duration: "5 min video", icon: "🎥", path: "#" },
      { title: "Understanding Wallets", duration: "4 min read", icon: "👛", path: "#" }
    ],
    tools: [
      { title: "Price Tracker", icon: "📊", path: "#" },
      { title: "Wallet Guide", icon: "👛", path: "#" },
      { title: "Explorer", icon: "🔍", path: "#" },
      { title: "Community", icon: "🌍", path: "#" }
    ],
    faq: [
      { question: "Is Bitcoin safe?", answer: "Yes, when stored properly. Bitcoin uses cryptography to secure transactions..." },
      { question: "Can I convert to cash?", answer: "Yes, through exchanges like Coinbase, Kraken, or peer-to-peer platforms..." },
      { question: "What if I lose my keys?", answer: "Your private keys are like passwords - if lost, funds cannot be recovered. Always keep backups..." }
    ]
  }
%>

<div class="min-h-screen">
  <!-- Hero Section -->
  <div class="bg-gradient-to-r from-orange-500 to-red-500 text-white">
    <div class="max-w-7xl mx-auto px-4 py-12">
      <h1 class="text-4xl font-bold mb-4">Bitcoin Learning Center</h1>
      <p class="text-xl opacity-90">Everything you need to know about Bitcoin</p>
    </div>
  </div>

  <!-- Quick Actions -->
  <div class="max-w-7xl mx-auto px-4 -mt-8 mb-8">
    <div class="card bg-white shadow-xl">
      <div class="card-body">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <%= link_to dashboard_path, class: "btn btn-lg btn-success" do %>
            <%= heroicon "currency-dollar", variant: :solid, class: "w-5 h-5" %>
            Check Balance
          <% end %>
          <button class="btn btn-lg btn-info" onclick="bitcoin_assistant_modal.showModal()">
            <%= heroicon "chat-bubble-left-right", variant: :solid, class: "w-5 h-5" %>
            Ask AI Assistant
          </button>
        </div>
      </div>
    </div>
  </div>

  <div class="max-w-7xl mx-auto px-4 pb-12">
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
      <!-- Main Content -->
      <div class="lg:col-span-2 space-y-8">
        <!-- Beginner Section -->
        <section>
          <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
            <span class="text-3xl">🌱</span>
            Start Here
          </h2>
          <div class="grid gap-4">
            <% sections[:beginner].each do |item| %>
              <div class="card bg-white shadow hover:shadow-lg transition-shadow">
                <div class="card-body">
                  <div class="flex items-center justify-between">
                    <div class="flex items-center gap-4">
                      <span class="text-2xl"><%= item[:icon] %></span>
                      <div>
                        <h3 class="font-semibold"><%= item[:title] %></h3>
                        <p class="text-sm text-gray-600"><%= item[:duration] %></p>
                      </div>
                    </div>
                    <%= link_to item[:path], class: "btn btn-ghost btn-sm" do %>
                      <%= heroicon "arrow-right", variant: :outline, class: "w-5 h-5" %>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        </section>

        <!-- Video Tutorials -->
        <section>
          <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
            <span class="text-3xl">🎥</span>
            Video Tutorials
          </h2>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div class="aspect-video bg-gray-200 rounded-lg flex items-center justify-center">
              <span class="text-4xl opacity-50">▶️</span>
            </div>
            <div class="aspect-video bg-gray-200 rounded-lg flex items-center justify-center">
              <span class="text-4xl opacity-50">▶️</span>
            </div>
          </div>
        </section>

        <!-- FAQ Section -->
        <section>
          <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
            <span class="text-3xl">❓</span>
            Common Questions
          </h2>
          <div class="join join-vertical w-full">
            <% sections[:faq].each_with_index do |faq, index| %>
              <div class="collapse collapse-arrow join-item border border-gray-300">
                <input type="radio" name="faq-accordion" <%= 'checked' if index == 0 %> />
                <div class="collapse-title text-lg font-medium">
                  <%= faq[:question] %>
                </div>
                <div class="collapse-content">
                  <p class="text-gray-700"><%= faq[:answer] %></p>
                </div>
              </div>
            <% end %>
          </div>
        </section>
      </div>

      <!-- Sidebar -->
      <div class="lg:col-span-1 space-y-6">
        <!-- Useful Tools -->
        <section>
          <h3 class="text-xl font-bold mb-4 flex items-center gap-2">
            <span class="text-2xl">🛠️</span>
            Useful Tools
          </h3>
          <div class="grid grid-cols-2 gap-3">
            <% sections[:tools].each do |tool| %>
              <%= link_to tool[:path], class: "card bg-white shadow hover:shadow-lg transition-all text-center" do %>
                <div class="card-body p-4">
                  <span class="text-3xl mb-2"><%= tool[:icon] %></span>
                  <h4 class="text-sm font-medium"><%= tool[:title] %></h4>
                </div>
              <% end %>
            <% end %>
          </div>
        </section>

        <!-- Need More Help? -->
        <section class="card bg-gradient-to-r from-orange-500 to-red-500 text-white">
          <div class="card-body">
            <h3 class="card-title text-white">Need More Help?</h3>
            <p>Our AI assistant can answer your specific questions about Bitcoin</p>
            <button class="btn btn-secondary" onclick="bitcoin_assistant_modal.showModal()">
              Chat Now
            </button>
          </div>
        </section>

        <!-- Community -->
        <section class="card bg-white shadow">
          <div class="card-body">
            <h3 class="card-title">Join the Community</h3>
            <p class="text-sm text-gray-700 mb-4">Connect with other Bitcoin enthusiasts</p>
            <div class="space-y-2">
              <%= link_to "Reddit r/Bitcoin", "https://reddit.com/r/bitcoin", class: "btn btn-sm btn-outline btn-block", target: "_blank" %>
              <%= link_to "Local Meetups", "#", class: "btn btn-sm btn-outline btn-block", target: "_blank" %>
              <%= link_to "Online Forums", "#", class: "btn btn-sm btn-outline btn-block", target: "_blank" %>
            </div>
          </div>
        </section>
      </div>
    </div>
  </div>
</div>

<!-- AI Assistant Modal -->
<dialog id="bitcoin_assistant_modal" class="modal">
  <div class="modal-box max-w-2xl h-[80vh] flex flex-col">
    <h3 class="font-bold text-lg mb-4">Bitcoin Assistant</h3>
    
    <!-- Chat Container -->
    <div class="flex-1 overflow-y-auto mb-4 space-y-4" id="chat-messages">
      <div class="chat chat-start">
        <div class="chat-bubble chat-bubble-primary">
          👋 Hi! I'm here to help you understand Bitcoin. What would you like to know?
        </div>
      </div>
    </div>
    
    <!-- Quick Questions -->
    <div class="grid grid-cols-2 gap-2 mb-4">
      <button class="btn btn-sm btn-outline" onclick="sendMessage('What is Bitcoin mining?')">
        What is Bitcoin mining?
      </button>
      <button class="btn btn-sm btn-outline" onclick="sendMessage('How do I buy Bitcoin?')">
        How do I buy Bitcoin?
      </button>
      <button class="btn btn-sm btn-outline" onclick="sendMessage('What is a blockchain?')">
        What is a blockchain?
      </button>
      <button class="btn btn-sm btn-outline" onclick="sendMessage('Is Bitcoin legal?')">
        Is Bitcoin legal?
      </button>
    </div>
    
    <!-- Input Form -->
    <form class="flex gap-2" onsubmit="event.preventDefault(); sendMessage(this.message.value); this.reset();">
      <input type="text" name="message" placeholder="Ask anything about Bitcoin..." class="input input-bordered flex-1" required>
      <button type="submit" class="btn btn-primary">Send</button>
    </form>
    
    <div class="modal-action mt-4">
      <form method="dialog">
        <button class="btn">Close</button>
      </form>
    </div>
  </div>
  <form method="dialog" class="modal-backdrop">
    <button>close</button>
  </form>
</dialog>

<script>
  function sendMessage(message) {
    // Placeholder implementation
    const chatContainer = document.getElementById('chat-messages');
    
    // Add user message
    chatContainer.innerHTML += `
      <div class="chat chat-end">
        <div class="chat-bubble">${message}</div>
      </div>
    `;
    
    // Add AI response (placeholder)
    setTimeout(() => {
      let response = "Great question! ";
      if (message.toLowerCase().includes('mining')) {
        response += "Bitcoin mining is the process of validating transactions and adding them to the blockchain...";
      } else if (message.toLowerCase().includes('buy')) {
        response += "You can buy Bitcoin through exchanges like Coinbase, Kraken, or peer-to-peer platforms...";
      } else {
        response += "Bitcoin is a decentralized digital currency that operates without a central authority...";
      }
      
      chatContainer.innerHTML += `
        <div class="chat chat-start">
          <div class="chat-bubble chat-bubble-primary">${response}</div>
        </div>
      `;
      
      chatContainer.scrollTop = chatContainer.scrollHeight;
    }, 500);
  }
</script>